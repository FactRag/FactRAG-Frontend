on:
  push:
    branches: [main]

jobs:
  build:
    name: create-package
    runs-on: ubuntu-latest

    steps:
      - name: Copy to another repo
        uses: andstor/copycat-action@v3
        with:
          personal_token: ${{ secrets.API_TOKEN_GITHUB }}
          src_path: build
          src_branch: main
          dst_path: /.
          dst_owner: FactRag
          dst_repo_name: FactRAG-Deploy
          dst_branch: main